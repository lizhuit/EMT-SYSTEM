import gestor.empresarial.contrato.Contrato;
import gestor.interfaz.*;//importamos la interfaz gráfica
public class Principal {
    public static void main(String []args){
        Loguin log = new Loguin();//se instancia Loguin

        /*solos login se mencina en el principal, ya que a partir de login llegan
        las demás ventanas*/
    }
}
